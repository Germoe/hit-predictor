from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project uses Billboard 200 and Spotify API data to make predictions on a song\'s potential to make it onto the Billboard 200 (become a mainstream hit).',
    author='Sebastian Engels',
    license='MIT',
)
